import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Victoria Gold - Premium Gold from Uganda & Congo",
  description: "Ethically sourced, certified gold from Uganda and Congo. Quality gold bars, coins, and custom orders. Trusted by businesses worldwide.",
  keywords: "gold, Uganda, Congo, precious metals, certified gold, gold bars, ethical sourcing, Victoria Gold",
  authors: [{ name: "Victoria Gold" }],
  icons: {
    icon: "/favicon.svg",
    apple: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
